# Claude Code instructions

Read `AGENT.md` before changing code.

Work only in the ownership area named in your task. Provider agents must not
change shared public contracts or the provider registry unless the integration
task explicitly requests it.

Use synthetic, provenance-documented fixtures. Never copy personal transcripts,
credentials, home paths, or private prompts into the repository.

Run `npm run check` and commit only the files owned by your task.
